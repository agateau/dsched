## Config file

~/.config/dsched/dsched.conf

    [task <name>]
    command=
    interval=

    # Optional: command which must succeed to run the task
    requires=

## Files

Log file

~/.cache/dsched/dsched.log
